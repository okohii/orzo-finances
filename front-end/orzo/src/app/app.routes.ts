import { Routes } from '@angular/router';
import { Home } from './pages/home/home';
import { App } from './app';

export const routes: Routes = [
    {
        path: '',
        component: Home,
    },
];
